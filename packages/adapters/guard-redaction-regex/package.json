{
  "name": "@opencoo/guard-redaction-regex",
  "version": "0.0.0",
  "private": true,
  "description": "Regex-based GuardAdapter (role: redaction) covering email, Polish PII (PESEL/NIP/REGON), IBAN, credit-card, and well-known secret-token shapes. Pure-function classify; engine handles persistence.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencoo/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "~5.7.2",
    "vitest": "^3.0.5"
  }
}
