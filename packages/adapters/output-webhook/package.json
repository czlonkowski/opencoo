{
  "name": "@opencoo/output-webhook",
  "version": "0.0.0",
  "private": true,
  "description": "OutputAdapter for generic webhooks — sends signed HTTP POST payloads to any external receiver with exponential-backoff retry and append-only delivery audit.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./testing": {
      "types": "./dist/testing/mock-http.d.ts",
      "default": "./dist/testing/mock-http.js"
    }
  },
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:contract": "vitest run -t 'outputAdapterContract'"
  },
  "dependencies": {
    "@opencoo/shared": "workspace:*",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "~5.7.2",
    "vitest": "^3.0.5"
  }
}
