/**
 * Engine-ingestion thin re-export of `postgresProbe` from
 * `@opencoo/shared/engine-scaffold`.
 */
export {
  postgresProbe,
  type PostgresProbeTarget,
} from "@opencoo/shared/engine-scaffold";
