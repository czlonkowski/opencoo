// Pipeline scaffold — lands in `feat(shared)` commits that follow.
// Empty-string and identity cases already pass; every other test is Red
// until the pipeline is wired in.
export function normalize(input: string): string {
  return input;
}
